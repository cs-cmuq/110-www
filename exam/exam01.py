
def clean_and_join(L, trash):
    return



def list_sort(L, s):
    return



def nested_lists(l, i, j, k):
    if len(l) >= 
        if len(l[i]) 
            if type(l[i][j]) =
                if len(l[i][j]
                    return 
    return
